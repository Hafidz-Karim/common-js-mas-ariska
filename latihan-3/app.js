const {kali, bagi} = require('./utils/calculator.js');
const {capitalize} = require('./utils/stringsUtils.js');

console.log(kali(5, 10));
console.log(bagi(50,5));

const data = capitalize('hafidzkarim')
console.log(data);
